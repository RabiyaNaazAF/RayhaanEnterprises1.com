 TODO: Implement AI Image-to-Image Generation in AIplan.tsx

## Steps to Complete

- [x] Create `server.js` for Node.js/Express backend with `/api/generate-design` endpoint
- [x] Update `package.json` to add backend dependencies (express, multer, replicate) and server script
- [x] Edit `src/pages/AIplan.tsx` to send FormData to backend instead of simulating
- [x] Install new dependencies using npm/bun
- [x] Create `.env` file for REPLICATE_API_TOKEN
- [ ] Test the backend endpoint
- [ ] Run the full app with frontend and backend
